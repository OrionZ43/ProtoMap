# Приведение ProtoMap в соответствие с Законом РБ № 99-З

Рабочий трекер. Ведётся до закрытия всех пунктов.
Начат: 2026-09-03. Последнее обновление: 2026-09-03.

## Источники

- Полный аудит: <https://claude.ai/code/artifact/8686e65d-b116-4011-b7b4-4164d268a643>
  (5 HIGH, 10 MEDIUM, 5 LOW; дата анализа 2026-09-02, против PP v4.1 и ToS v4.0)
- Постановка задачи: `docs/аудит/claude-code-prompt.md`
- Новые тексты: `docs/аудит/privacy_policy_v5.xml`, `docs/аудит/terms_of_service_v5.xml`
- Оговорка из аудита: это не юридическая консультация. Перед публикацией
  текстов проверить у практикующего белорусского юриста по IT-праву.
  Закон № 99-З изменён Законом от 16.03.2026 № 134-З — сверить нумерацию
  статей на pravo.by.

## Установленные факты

### Health Connect не используется — подтверждено трижды

| Проверка | app | wearos | gradle |
| --- | --- | --- | --- |
| `android.permission.health.READ_STEPS` | нет | нет | — |
| `androidx.health.connect:connect-client` | — | — | **нет** |
| `FOREGROUND_SERVICE_HEALTH` | нет | **есть** | — |
| `ACTIVITY_RECOGNITION` | есть | есть | — |
| `uses-feature hardware.sensor.stepcounter` | есть | — | — |

Шаги читаются только с аппаратного датчика. Денис подтвердил:
«Health Connect убирай, у нас его нет, нас же за него сносили».

**`FOREGROUND_SERVICE_HEALTH` живёт в модуле `wearos`.** Это НЕ Health Connect,
а тип foreground-сервиса (обязателен с Android 14 для FGS типа `health`).
В `settings.gradle.kts` модуль подключён, но в `dynamicFeatures` модуля `app`
указан только `:admin` — манифест `wearos` в APK телефона не мержится.
Денис: «Wearos-модуль вообще пока не трогай, он не готов от слова полностью».

→ **Решение:** документы описывают только то, что реально отгружается сегодня,
то есть набор разрешений модуля `app`. Про `wearos` в документах не пишем ничего.
Когда модуль поедет в релиз — вернуться сюда и добавить раздел.

### Документы v5 описывали архитектуру часов, а не телефона

Названные в текстах v5 `StepCounterService`, `StepCounterBootReceiver` и
`WalkTrackingService` **в телефонном приложении не существуют**. Они есть только
в модуле `wearos` как `WearStepCounterService` и `WearBootReceiver`.

Что в `app` на самом деле:

| Заявлено в v5 | Фактически в `app` |
| --- | --- |
| `StepCounterService`, FGS типа health | `WalkSessionService`, `foregroundServiceType="location"` |
| `WalkTrackingService` | не существует |
| `StepCounterBootReceiver` + `RECEIVE_BOOT_COMPLETED` | не существует, разрешение не запрашивается |

Причина: аудит, судя по всему, делался по `mobile.xml` — это дамп всего проекта
разом, включая незаконченный `wearos`, и модули в нём неразличимы. Код часов
попал в описание телефона.

### Разрешения: документы против манифеста `app`

Есть в манифесте, отсутствует в текстах v5:

- `POST_PROMOTED_NOTIFICATIONS`
- `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`
- `FOREGROUND_SERVICE` (общий, без типа)

Есть в текстах v5, отсутствует в манифесте `app`:

- `android.permission.health.READ_STEPS` — удалить (нигде не используется)
- `FOREGROUND_SERVICE_HEALTH` — удалить (только `wearos`, не отгружается)
- `RECEIVE_BOOT_COMPLETED` — удалить (только `wearos`)

### На вебе три системы юридических документов

1. `/privacy-policy`, `/terms-of-service` → XML из `system/licenses`. **Настоящая.**
2. `/app-privacy`, `/app-terms` → рендерятся из i18n-ключей `app_privacy_policy`
   и `app_terms`, которые превращены в заглушки с полем `_merged: true`.
   **Страницы живы и сломаны** — ключи вроде `section_geo_title` не существуют.
3. Ключи `privacy` (20 КБ) и `terms` (14 КБ) в `ru.json`/`en.json` —
   старые полные тексты, ни один компонент их не рендерит.

→ Пункты 2 и 3 удалить. Но сначала выяснить, какой URL указан в Google Play
как ссылка на политику конфиденциальности (см. открытые вопросы).

### Что уже сделано и переделывать не надо

- Правила Firestore для `system/licenses`: `read: if true`, `write: if isAdmin()` — верно.
- `LegalDocRenderer` + `legalLoader` поддерживают все теги новых документов
  (`title/section/subsection/paragraph/bullet/alert/highlight/contact`). Работы ноль.
- На `/banned` кнопка апелляции уже есть (ссылка на `t.me/Orion_Z43`).
  Нужно только исключение для п. 2.3 (безопасность детей).

### Чего в проекте нет вообще

- **Никаких возрастных проверок.** Grep по `adultConfirmed`, `age16`, `birthdate`,
  «возраст» по всему `src/` — ноль совпадений. И 16+, и 18+ существуют только как текст.
- **Никакого серверного журнала согласий.** `LegalUpdateBanner` пишет принятую
  версию в `localStorage` и всё. Ни одной серверной записи о принятии не существует.
  По п. 7 ст. 5 доказывать нечем в принципе.
- **Симуляция игр не является азартной игрой.** `purchaseShopItem` тратит только
  ProtoCoins, платёжной интеграции нет, p2p-передачи коинов нет. Триггеры из L-2
  (ввод фиата, вывод, p2p) не наступили.

## Ошибки в постановке задачи — исправлены здесь

| В промпте | Верно |
| --- | --- |
| Cloud Functions в `europe-central2` | **`europe-west1`.** `europe-central2` — регион Firestore. Scheduled-функцию из задачи 6 деплоить в `europe-west1` |
| «Атрибут `version` в XML должен совпадать со строкой версии» | Невыполнимо дословно: в файлах `version="v5.0 \| [ДАТА]"`, поле просят `"5.0"`. Атрибут нигде не используется — страница берёт `data.version` из поля Firestore. Оставляем как есть |
| Задача 5 подаётся как отсутствующая | Кнопка апелляции уже есть, нужно только исключение п. 2.3 |
| Задача 0 — «убрать Health Connect из манифеста» | Манифест в репозитории Дениса, здесь его нет. В `app` разрешений Health Connect и не было |

## Решения

**Возраст (2026-09-03).** Гейт 18+ на «The Glitch Pit» **не делаем**, поле
«дата рождения» **не заводим**. Обоснование:

- Юридического требования 18+ нет — это самоназначенная цифра из собственного ToS.
  Указ № 9 не применяется: нет ввода фиата, вывода и p2p (аудит, L-2, LOW).
- Дата рождения без реакции на неё нарушает минимизацию (п. 5 ст. 4) и создаёт
  журнал записей, доказывающих, что оператор знал возраст субъекта.
- Самодекларация галочкой «мне исполнилось 16 лет» имеет ту же юридическую силу
  и не сохраняет ПД.
- Расхождение с Google Play решается наоборот: честно заполнить анкету IARC,
  получить рейтинг и **эту цифру записать в документы**.

→ Из ToS убрать «доступ с 18 лет», привести к рейтингу стора. Задача 4 отменена,
заменена на правку текста.

**Реквизиты оператора (2026-09-03).** Ковалёв Денис Дмитриевич, г. Минск.
Публикация города согласована: «метка у меня и так стоит, город уже очевиден».

**Дата вступления в силу (2026-09-03): 14.09.2026**, понедельник — 11 дней от
сегодня, п. 6.2 ToS выполняется. ⚠️ Отсчёт идёт от **фактической заливки**,
поэтому залить надо не позже 04.09; если заливка съедет на 05.09 и позже, до
14.09 останется меньше десяти дней и документ нарушит собственный пункт.

**Возраст (2026-09-03, уточнено).** В Play у приложения рейтинг **ESRB Mature 17+**.
В документах фигурировали три разных числа разной природы, и путались они между собой:

| Число | Откуда | Что с ним сделали |
| --- | --- | --- |
| 16 | п. 9 ст. 5 Закона № 99-З — возраст, до которого согласие даёт законный представитель. Норма закона | Осталось, но переформулировано как отдельное правовое замечание, а не как ценз доступа |
| 17 | Фактический рейтинг в Google Play (ESRB M) | Стало единственным цензом доступа — и в ToS, и в Политике |
| 18 | Самоназначенный гейт на «The Glitch Pit» | Убрано. Ни на чём не основано: ProtoCoins нельзя купить и нельзя вывести, Указ № 9 не применяется |

Отдельного возрастного ограничения у «The Glitch Pit» больше нет: раздел находится
внутри приложения, которому уже присвоен рейтинг 17+.

Идентификатор согласия переименован `age16` → **`age_minimum`**: число возраста
поменялось за один день, а id согласия попадает в журнал навсегда. Записей ещё нет,
так что переименование бесплатное.

⚠️ **Стоит отдельно перепроверить сам рейтинг.** ESRB Mature 17+ — тяжёлая
категория для карты с чатом и мини-играми на виртуальной валюте; обычно её дают за
насилие, кровь или сексуальный контент. Скорее всего рейтинг подняли ответы анкеты
IARC про симуляцию азартных игр в сумме с пользовательским контентом и геолокацией.
Практическое следствие: приложение с M 17+ скрыто от заметной доли пользователей Play
и от аккаунтов с родительским контролем — это одно из механических объяснений низкого
числа установок. Пересмотреть ответы анкеты стоит, но честно, а не ради цифры.

**Стиль документов (2026-09-03).** Не описывать то, что не используется.
Орион: «не надо писать если что-то не используется, это только нагружает больше,
используется — пишем, не используется — убираем». Первая редакция правки
оставляла явные отрицания («Health Connect не используется») в расчёте на
ревьюера Play — отменено, отрицания вычищены полностью.

## План по коммитам

- [x] **0. Тексты v5: разрешения** — сделано 2026-09-03:
  - [x] вычищен Health Connect и `health.READ_STEPS` (0 упоминаний в обоих файлах)
  - [x] `FOREGROUND_SERVICE_HEALTH` → `FOREGROUND_SERVICE + FOREGROUND_SERVICE_LOCATION`
        с описанием реального `WalkSessionService` (тип `location`)
  - [x] `RECEIVE_BOOT_COMPLETED` (только в `wearos`) → заменён на
        `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`
  - [x] `WalkTrackingService` → `WalkSessionService`
  - [x] добавлены `POST_PROMOTED_NOTIFICATIONS`, `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`
  - [x] подставлены ФИО и город
  - [x] оба документа прогнаны через `fast-xml-parser` по логике `legalLoader.ts`:
        PP — 150 узлов, ToS — 118, все с парами `ru`/`en`, неизвестных тегов нет
  - [x] **дата вступления в силу** — 14.09.2026 / 14 September 2026, подставлена
  - [x] **18+ убрано, ценз приведён к 17+** — предупреждение о симуляции, заверение
        пользователя, алерт раздела «The Glitch Pit» и раздел 11 Политики;
        чекбокс согласия и три локали обновлены на 17
- [x] **0a. Починить `/banned`** — сделано 2026-09-03. Убран мёртвый
      `data.reason || get(t)(...)` (сервер и так подставляет `'Protocol Violation.'`),
      поэтому `ReferenceError` больше нет; добавлен `clearInterval` при размонтировании;
      эмодзи 🔒 заменён на SVG Lucide «lock». `svelte-check` по файлу чист, тесты 85/85.
      Закрывает `.claude/rules/known-issues.md` № 2.
- [x] **1. Скрипт заливки** — `scripts/upload-legal-docs.mjs`, сделан 2026-09-03.
      Сухой прогон по умолчанию, запись требует `--apply`. Перед записью валидирует
      документы по логике `legalLoader.ts` и **отказывается заливать при
      незаполненных плейсхолдерах** — проверено, сейчас отбивает по `[ДАТА]`.
  - [ ] **ЗАЛИТЬ. Дата 14.09 требует публикации не позже 04.09** — сегодня.
        Позже придётся двигать на 21.09. Команда: `node scripts/upload-legal-docs.mjs --apply`
- [x] **2. Экран согласия (веб)** — сделан 2026-09-03.
      Блок «Прежде чем вы дадите согласие» показывается целиком, без аккордеона.
      Четыре обязательных чекбокса: `age16`, `core_processing`, `cross_border`, `tos`.
      Ни один не предзаполнен, обе кнопки регистрации заблокированы до отметки всех.
      Согласия пишутся и на обычной регистрации, и на регистрации через Google.
      Галочки переносятся через `sessionStorage` при уходе на `signInWithRedirect`
      (иначе регистрация по редиректу упиралась бы в непроходимую проверку).
      `activity_data` на вебе НЕ показывается: шагомера на вебе нет.
      Ключи добавлены в `ru`, `en` и `ru_april` — в первоапрельской локали текст
      обычный, не стилизованный: согласие, сформулированное шуткой, не однозначно.
  - [ ] **Android-половина** — за Денисом
- [x] **3. Журнал согласий** — сделан 2026-09-03. `functions/src/consents.ts`:
      `recordConsents`, `revokeConsent`, `getMyConsents` + `anonymizeConsentsOnDelete`.
      Коллекция `consents` верхнеуровневая, в правилах `allow write: if false`.
      Версию документа определяет сервер по `system/licenses` — клиент не может
      заявить, что именно он принял. `deleteAccount` теперь обезличивает записи,
      а не удаляет. Обязательные согласия отдельно не отзываются: единственный
      путь — удаление аккаунта, функция об этом прямо сообщает.
      В `recordConsents` намеренно НЕТ `assertEmailVerified`: на регистрации почта
      заведомо не подтверждена.
  - [x] развёрнуто 2026-09-04: функции в europe-west1, правила в Firestore
- [x] **4. ~~Гейт 18+~~** — отменено, см. «Решения».
  - [x] 18+ убрано, ценз приведён к 17+ (ESRB Mature 17+ из Play)
- [x] **5. Обжалование блокировок** — сделано 2026-09-03. Добавлено поле
      `banCategory` (`general` / `child_safety`), выставляется чекбоксом в
      админке. На `/banned` при `child_safety` вместо кнопки — текст об
      окончательности решения. Старые баны поля не имеют и считаются обжалуемыми,
      то есть поведение для них не изменилось.
- [x] **6. Автоудаление по срокам** — развёрнуто, но **работает вхолостую**:
      переменная `RETENTION_APPLY` не выставлена, функция только считает и пишет
      в лог. Это сделано намеренно — сначала посмотреть по логам, что счётчики
      совпадают с ожиданием. Сделано 2026-09-03,
      `functions/src/retention.ts`, `enforceRetention`, ежедневно в 04:00
      по Минску, регион наследуется из `index.ts` (**`europe-west1`**).
      **По умолчанию ничего не удаляет** — только считает и пишет в лог.
      Включается переменной окружения `RETENTION_APPLY=true`.
      Сроки собраны в константу `RETENTION` в одном месте.
      Обрабатываются только коллекции с проверенной по коду схемой:
      `consents`, `stepper_idempotency`, `2fa_codes`, `2fa_cleared`,
      `claim_history` (collectionGroup).
  - [ ] **`auth_logs` не трогается** — там ПД (uid, email каждого входа) лежат
        внутри массива `login_history`, а не в самом документе. Нужна чистка
        элементов массива, а на этих данных admin-модуль строит детект нарушений.
        Схема из Android-репозитория — к Денису.
- [x] **7. Уборка** — сделано 2026-09-03:
  - [x] удалены маршруты `/app-privacy` и `/app-terms` (Орион подтвердил, что Google
        смотрит на общие документы и для сайта, и для приложения)
  - [x] удалены мёртвые ключи `privacy`, `terms`, `app_privacy_policy`, `app_terms`
        из трёх локалей — 78 КБ, диff 19 вставок / 884 удаления
  - [x] добавлены отсутствовавшие ключи `privacy_policy` и `terms_of_service`:
        страницы на них ссылались, но в локалях их не было — в заголовке вкладки
        показывался сам ключ
  - [x] «казино» → «мини-игры» в `meta description` / OG / twitter
  - [ ] убрать «казино» из описания в Google Play — за Орионом
  - [ ] Android тянет документы из `system/licenses` — за Денисом
  - [ ] повторное принятие срабатывает на `4.0 → 5.0` и `4.1 → 5.0` — за Денисом

## Найдено попутно

- [x] **Дыра в безопасности: `assertNotBanned` был no-op в пяти функциях.**
      `assertNotBanned(uid)` вызывался со строкой вместо `request` в
      `toggleCommentLike`, `startCrashGame`, `synthesizeArtifact`,
      `playSlotMachine`, `playCoinFlip`. На строке `request.auth?.token` и
      `request.auth?.uid` дают `undefined`, поэтому обе проверки — и клейм токена,
      и запись в БД — молча пропускались. **Забаненный пользователь мог играть
      в казино и синтезировать артефакты.** Компилятор не видел ошибки, потому что
      параметр был объявлен `any`. Исправлены все вызовы, параметр типизирован
      структурным типом `BanCheckable` — теперь такая передача не соберётся.
      Ровно тот случай, о котором предупреждает `.claude/rules/economy.md`:
      третий рубеж защиты от бана существовал только на бумаге.
- [x] **`/banned` падала при загрузке** — `get` и `t` не импортированы, при этом
      сервер и так подставляет причину по умолчанию, то есть ветка была мёртвой.
      Забаненного редиректило на страницу с `ReferenceError`.
      Закрывает `known-issues.md` № 2.
- [x] **Модалка 2FA на регистрации падала** — `transition:fade` и `transition:slide`
      без импорта из `svelte/transition`. Закрывает `known-issues.md` № 3.
- [x] **Утечка таймера на `/banned`** — `setInterval` эффекта печатной машинки
      не очищался при размонтировании.

## Открытые вопросы

1. ~~Какой URL указан в Google Play как политика конфиденциальности~~ —
   закрыт 2026-09-03: Google смотрит на общие документы и для сайта, и для
   приложения. Легаси-маршруты удалены.
2. ~~Дата вступления в силу~~ — закрыт 2026-09-03: **14.09.2026**, понедельник.
3. ~~Возрастной рейтинг~~ — закрыт 2026-09-03: ESRB Mature 17+, документы приведены
   к этой цифре. Осталось необязательное: перепроверить ответы анкеты IARC, см. «Решения».
4. `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` — под что запрашивается? В текстах
   описано как «чтобы Android не останавливал учёт прогулки в фоне» — правдоподобно
   по коду (`WalkSessionService`), но не подтверждено. К Денису.
5. **Чистка `login_history` в `auth_logs`** — см. пункт 6 плана. К Денису.

## Что осталось развернуть

Всё написанное лежит в рабочем дереве и собирается, но **на проде ничего из
этого ещё нет**:

```bash
npm --prefix functions run build          # проходит
firebase deploy --only functions:recordConsents,functions:revokeConsent,functions:getMyConsents,functions:getMyConsents,functions:enforceRetention,functions:deleteAccount
firebase deploy --only firestore:rules    # добавлена коллекция consents
node scripts/upload-legal-docs.mjs        # сухой прогон; --apply после даты
```

⚠️ `firestore.rules` до сих пор не закоммичен целиком — см.
`.claude/rules/known-issues.md` № 5. Правило для `consents` добавлено в тот же
незакоммиченный файл.
