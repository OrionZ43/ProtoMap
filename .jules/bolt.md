## 2024-05-24 - [Firestore] **Урок:** Запрос коллекций без ограничения (`limit`) приводит к избыточному расходу чтений БД, загрузке всей истории и снижению производительности. **Действие:** Всегда добавлять `limit()` (например, `limit(50)`) в запросы к бесконечным коллекциям вроде постов и сообщений (особенно в `onSnapshot`).## 2026-04-22 -[Vitest Setup] **Урок:** [Для тестирования SvelteKit сторов, взаимодействующих с Firebase, важно правильно мокать /environment и модули firebase, чтобы тесты могли работать в изолированном окружении jsdom.] **Действие:** [Добавлена настройка Vitest и моки для стора чата.]
