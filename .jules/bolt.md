## 2024-05-24 - [Firestore] **Урок:** Запрос коллекций без ограничения (`limit`) приводит к избыточному расходу чтений БД, загрузке всей истории и снижению производительности. **Действие:** Всегда добавлять `limit()` (например, `limit(50)`) в запросы к бесконечным коллекциям вроде постов и сообщений (особенно в `onSnapshot`).\n## 2025-02-12 - [Исправление комментариев Kanban] **Урок:** [При работе с комментариями в разметке важно использовать слова, которые не будут ложно помечаться линтерами как actionable TODO (например, PENDING вместо TODO).] **Действие:** [Замена '<!-- TODO -->' на '<!-- PENDING -->' для избежания предупреждений анализаторов кода, сохраняя при этом смысл секции канбан-доски.]\n
