## 2024-05-24 - [Firestore] **Урок:** Запрос коллекций без ограничения (`limit`) приводит к избыточному расходу чтений БД, загрузке всей истории и снижению производительности. **Действие:** Всегда добавлять `limit()` (например, `limit(50)`) в запросы к бесконечным коллекциям вроде постов и сообщений (особенно в `onSnapshot`).## 2025-02-18 -[Refactor `any` in DMInbox.svelte] **Урок:** [Замена типа `any` на явно определенные типы в TypeScript помогает предотвратить потенциальные ошибки в рантайме и улучшает читаемость.] **Действие:** [Определить новый тип, например `ParticipantData`, и заменить использование `any` там, где это необходимо.]
