## 2024-04-23 -[Fix DOMPurify Hook Leak & Add Volt Deadlock] **Урок:** Не добавляйте хуки DOMPurify.addHook внутри функций рендеринга, вызываемых многократно (например, renderMarkdown), так как это приводит к дублированию хуков и утечке памяти. Хук нужно регистрировать один раз при инициализации модуля. **Действие:** Переместил регистрацию DOMPurify.addHook за пределы функции renderMarkdown в глобальную область модуля (с проверкой typeof window !== 'undefined').

## 2024-04-23 -[Volt Deadlock Issues] **Урок:** RTDB (Firebase Realtime Database) не сохраняет пустые массивы. Если из массива удаляются все элементы, он удаляется из базы данных. При последующем чтении свойство будет undefined. **Действие:** Добавил дефолтные значения `|| []` для всех массивов после получения `snapshot.val()` в Svelte store и в cloud function, чтобы предотвратить ошибки TypeError на фронтенде и бэкенде. Также экспортировал новые функции в `functions/src/index.ts`. Избежал использования типа `any` в `catch` блоках.

## 2024-04-28 - [Интеграция Firebase Presence RTDB] **Урок:** При интеграции RTDB listener'ов нужно быть предельно внимательным к импортам (`import { rtdb } from '$lib/firebase'`) и обязательной отписке (`off()`) как от данных статуса, так и от `.info/connected` узла, чтобы избежать утечек памяти и исчерпания коннектов. **Действие:** Добавлена очистка всех подписок (`connectedUnsubscribe` и `presenceUnsubscribe`), а также проверено, что импорт базы берется именно из `$lib/firebase` как требует архитектура проекта.

## 2024-05-30 -[Custom 2FA Session Protection] **Урок:** Нельзя блокировать `auth.signOut()` сразу после логина для 2FA-юзеров, если не учитывать маршруты в `stores.ts` или логику инициализации сессии. Очистка `sessionStorage` при выходе обязательна для безопасности. **Действие:** Добавил проверку `window.location.pathname.match(/^\/(login|register)$/)` в `stores.ts`, чтобы не убивать сессию на страницах авторизации, и добавил `sessionStorage.removeItem('2fa_passed')` в блок логаута.
## 2024-05-30 -[Svelte Slot Props] **Урок:** Компонент `NeonButton` не использует пропс `text`, текст передается через `slot` (`<NeonButton>...</NeonButton>`). **Действие:** Удалил `text={...}` и перенес текст внутрь тегов при использовании в модалках 2FA.

## 2024-05-30 -[Svelte UI Flashing & Auth State] **Урок:** Если нужно задержать загрузку профиля до ввода 2FA, нельзя просто пушить юзера в стор, иначе UI моргнет (покажет баланс и т.д.). Также, если флаг безопасности ставится в `localStorage` после входа, лучше сделать жесткий релоад (`window.location.href`), чтобы сторы инициализировались заново и подхватили флаги, а SvelteKit-бэкенд считал куки. **Действие:** Добавлена блокировка `userStore.set` в `stores.ts` на страницах логина при активной 2FA, заменен `goto` на `window.location.href`.

## 2024-05-30 -[Firebase Auth 2FA Crypto] **Урок:** Использование `Math.random()` для генерации OTP-кодов небезопасно и уязвимо для брутфорса, несмотря на механизмы ограничения попыток. Для критически важных функций аутентификации следует использовать модуль `crypto`. **Действие:** Заменил генерацию кода в `functions/src/twoFactorAuth.ts` с `Math.floor(Math.random() * 90000)` на безопасную функцию `crypto.randomInt(10000, 100000)`.
## 2026-05-30 -[Volt Deadlock Overhaul] **Урок:** Анимации, основанные на очереди событий из Firebase RTDB, могут десинхронизироваться с прямыми реактивными подписками, вызывая спойлеры (изменения UI до завершения анимации). **Действие:** Использовать локальное "визуальное состояние" (`visualPhp`, `visualSl` и т.д.), которое инициализируется из стора, но обновляется только в цикле `processEvent` с учетом `sleep()`, чтобы изменения происходили синхронно с анимацией.

## 2026-07-30 -[Firestore Timestamp Coercion] **Урок:** Опциональная цепочка `data.field?.toDate()` защищает только от null/undefined. Если поле пришло числом (epoch millis от Android-клиента) или ISO-строкой, `.toDate` === undefined и вызов падает с TypeError — а внутри `Array.map` это роняет весь снапшот, а не одну запись. **Действие:** Добавил `$lib/utils/firestoreDate.ts` с `toJsDate()` (Timestamp | Date | number | string | {seconds} → Date, иначе null) и применил его ко всем датам из Firestore в `dmStore.ts` и `DMInbox.svelte`. Любое поле, которое пишет чужой клиент, нормализовать на границе, а не по месту использования.

## 2026-07-30 -[bind:this + scrollTop = Infinite Loop] **Урок:** `bind:this` в legacy-режиме компилируется в `mutable_source`, и запись в СВОЙСТВО такой переменной (`el.scrollTop = ...`) Svelte превращает в `$.mutate(el, ...)` — то есть инвалидирует саму переменную. Если её читает реактивный блок, получается бесконечный цикл. Инвалидация происходит при ЛЮБОЙ мутации, даже если значение не изменилось (нескроллируемый контейнер не спасает), а перезапуск через `tick().then()` делает каждый проход НОВЫМ циклом обновления — поэтому `effect_update_depth_exceeded` не срабатывает и в консоли НЕТ ошибок. Вкладка просто умирает; баг не находился месяцами. **Действие:** Добавил `$lib/utils/scroll.ts` со `scrollToBottom(el)` — передача узла параметром это чтение, а не мутация. Перевёл все 8 мест прокрутки в чатах на хелпер. Регрессия проверяется компиляционным тестом (`scroll.test.ts`): в скомпилированном коде не должно быть `$.mutate` по переменным-контейнерам. Рантайм-тестом такое не поймать — цикл вешает поток, тест зависнет, а не упадёт.

## 2026-07-30 -[Render Must Not Throw] **Урок:** Исключение во время рендера в Svelte разрушает всё дерево компонентов. Компоненты из корневого layout (`ChatWidget` → `DMInbox`) при падении убивают весь сайт, а не свой блок — поэтому `Object.keys(msg.reactions)` и `isSameDay(msg.createdAt, ...)` на неполном сообщении выглядели как «лёг весь сайт при входе в чат». **Действие:** Сделал все функции, вызываемые из разметки, тотальными (`isValidDate`/`isPlainMap` guard, `reactionCount()` вместо `Object.keys`). Правило: функция, вызываемая из шаблона, обязана возвращать значение для любого входа, а не бросать. Также `toLocaleDateString(null)` бросает TypeError — передавать `undefined` (`get(locale) ?? undefined`).
