## 2024-05-24 - [Firestore] **Урок:** Запрос коллекций без ограничения (`limit`) приводит к избыточному расходу чтений БД, загрузке всей истории и снижению производительности. **Действие:** Всегда добавлять `limit()` (например, `limit(50)`) в запросы к бесконечным коллекциям вроде постов и сообщений (особенно в `onSnapshot`).## 2024-05-18 -[TypeScript Типы в SvelteKit] **Урок:** [При импорте типов из `+page.server.ts` в Svelte компоненты, тип может быть недоступен напрямую через `./$types`, если он не возвращается напрямую в `LoadData` в виде самостоятельного экспорта. Безопаснее импортировать тип напрямую из `./+page.server`.] **Действие:** [Всегда проверять пути импорта типов с помощью `npx svelte-check`, даже если в задании указан конкретный путь, который может оказаться нерабочим.]
