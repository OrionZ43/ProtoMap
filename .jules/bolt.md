## 2024-05-24 - [Firestore] **Урок:** Запрос коллекций без ограничения (`limit`) приводит к избыточному расходу чтений БД, загрузке всей истории и снижению производительности. **Действие:** Всегда добавлять `limit()` (например, `limit(50)`) в запросы к бесконечным коллекциям вроде постов и сообщений (особенно в `onSnapshot`).## 2026-04-23 -[Security] **Урок:** [Firestore rules hasAny blacklist should include all critical user profile fields to prevent SDK bypass.] **Действие:** [Added username, avatar_url, etc. to users update hasAny rule and added author_uid check to chat message update rule.]
