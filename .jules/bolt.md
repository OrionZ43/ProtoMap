## 2024-05-24 - [Firestore] **Урок:** Запрос коллекций без ограничения (`limit`) приводит к избыточному расходу чтений БД, загрузке всей истории и снижению производительности. **Действие:** Всегда добавлять `limit()` (например, `limit(50)`) в запросы к бесконечным коллекциям вроде постов и сообщений (особенно в `onSnapshot`).## 2026-04-22 - Refactoring Auth Sync\n**Урок:** Избегание дублирования данных и гонок (race conditions) при обновлении сторов Firebase Auth. Принудительное обновление через единую функцию лучше ручных хаков.\n**Действие:** Вынесена логика синхронизации userStore в `refreshUserStore()` для вызова после регистрации.
