## 2024-05-24 - [Firestore] **Урок:** Запрос коллекций без ограничения (`limit`) приводит к избыточному расходу чтений БД, загрузке всей истории и снижению производительности. **Действие:** Всегда добавлять `limit()` (например, `limit(50)`) в запросы к бесконечным коллекциям вроде постов и сообщений (особенно в `onSnapshot`).
## 2026-04-22 -[isSafeUrl Tests and Bugfix] **Урок:** [При написании тестов был найден баг, при котором isSafeUrl не проверяла пробелы вначале URL: функция trim-ила URL в переменную 'lower', но regex-сравнение шло с оригинальным 'url'.] **Действие:** [Был исправлен баг в isSafeUrl с применением 'lower' к regex-тесту, а также добавлены юнит-тесты Vitest для проверки корректной обработки пробелов, протоколов и невалидных значений.]
