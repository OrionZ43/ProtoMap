/**
 * Подписи к отправленным файлам.
 *
 * Наборы разделены по источникам: под роликом с YouTube и под картинкой с e621
 * уместны разные шутки. NSFW-набор попадает только в личный чат — в ProtoMap
 * такие источники вообще не качаются (см. `download_nsfw` в реестре бота).
 *
 * Дописывать сюда — нормально, никакой логики от конкретных строк не зависит.
 * Ограничение одно: подпись к медиа в Telegram не длиннее 1024 символов.
 */

const BOT = "@OrionNeurobot";

const REGULAR = [
    `Скачал за вас. Могли бы и сами, но лень — это святое`,
    `${BOT}: единственный сотрудник, который работает`,
    `Потрачено сорок мегабайт трафика Дениса. ${BOT}`,
    `${BOT}. Единственная фича, которая не сломалась`,
    `Скачано быстрее, чем вышло обновление ProtoMap`,
    `YouTube решил, что я человек. Обманул. ${BOT}`,
    `Донёс. Не уронил. Горжусь собой. ${BOT}`,
    `${BOT} работает за электричество и признание. Признания пока нет`,
    `Пока вы спали, я качал. ${BOT}`,
    `Открывать ссылку не пришлось. Вы сэкономили две секунды. ${BOT}`,
    `${BOT}: краду видео, чтобы вы не покидали чат`,
    `Работаю на чужом ноутбуке в чужой квартире. ${BOT}`,
    `Изъято из интернета силами ${BOT}`,
    `${BOT}. Задача выполнена, смысл не найден`,
];

const NSFW = [
    `${BOT} скачал и сделал вид, что не смотрел`,
    `Я бот, у меня нет глаз. И слава богу. ${BOT}`,
    `${BOT}: это лежало в интернете, я тут ни при чём`,
    `Доставлено. Логи почищены. ${BOT}`,
    `${BOT} видел ваш запрос. ${BOT} ничего не скажет`,
    `Скачано. Осуждение в мой функционал не входит`,
    `И вот на это ушли чьи-то вечера. ${BOT}`,
    `${BOT} принёс и молча ушёл`,
    `Без комментариев. Совсем. ${BOT}`,
    `${BOT}: я не выбирал эту работу`,
];

export function captionFor(nsfw: boolean): string {
    const pool = nsfw ? NSFW : REGULAR;
    return pool[Math.floor(Math.random() * pool.length)];
}
