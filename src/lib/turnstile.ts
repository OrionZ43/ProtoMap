import { dev } from '$app/environment';

/**
 * Ключ виджета Cloudflare Turnstile для форм входа и регистрации.
 *
 * В боевой сборке — настоящий. В разработке — официальный тестовый ключ
 * Cloudflare, который проходит проверку всегда.
 *
 * Зачем: боевой ключ выдаёт интерактивную галочку «Verify you are human»,
 * и локальная разработка спотыкается о неё на каждом входе. Автоматические
 * прогоны браузером она не пропускает вовсе — что логично, для того и сделана.
 *
 * ⚠️ Не для всякой формы. Здесь токен нигде не проверяется сервером: это
 * заслон от ботов на клиенте. А в `verify-chat` токен уходит на проверку
 * в `PRIVATE_TURNSTILE_SECRET_KEY`, и тестовому ключу нужен парный тестовый
 * секрет — иначе сервер отвергнет заведомо валидный токен. Поэтому та форма
 * держит боевой ключ у себя и сюда не переведена.
 *
 * Список тестовых ключей: developers.cloudflare.com/turnstile/troubleshooting/testing/
 */
export const TURNSTILE_SITE_KEY = dev
    ? '1x00000000000000000000AA'
    : '0x4AAAAAACYHm8usBkEdoF37';
